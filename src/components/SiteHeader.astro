<header class="site-header">
  <div class="container header-inner">
    <a class="wordmark" href="/" aria-label="Ryan Wenstrom home">
      Ryan Wenstrom
    </a>

    <nav class="site-nav" aria-label="Primary navigation">
      <a href="/#projects">Projects</a>
      <a class="nav-optional" href="/#experience">Experience</a>
      <a href="/#about">About</a>
      <a href="/resumes/RyanWenstrom.Resume.pdf" target="_blank">
        Resume
      </a>
      <a
        class="nav-optional"
        href="https://www.linkedin.com/in/ryanwenstrom/"
        target="_blank"
        rel="noopener noreferrer"
      >
        LinkedIn
      </a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(247, 248, 250, 0.96);
    border-bottom: 1px solid var(--color-border);
    backdrop-filter: blur(10px);
  }

  .header-inner {
    min-height: 72px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;
  }

  .wordmark {
    color: var(--color-text);
    font-size: 1rem;
    font-weight: 800;
    text-decoration: none;
    letter-spacing: -0.02em;
    white-space: nowrap;
  }

  .site-nav {
    display: flex;
    align-items: center;
    gap: 28px;
  }

  .site-nav a {
    color: var(--color-text-muted);
    font-size: 0.9rem;
    font-weight: 650;
    text-decoration: none;
    transition: color 160ms ease;
  }

  .site-nav a:hover,
  .site-nav a:focus-visible {
    color: var(--color-accent-dark);
  }

  @media (max-width: 720px) {
    .header-inner {
      min-height: 64px;
      gap: 18px;
    }

    .site-nav {
      gap: 16px;
    }

    .site-nav a {
      font-size: 0.82rem;
    }

    .nav-optional {
      display: none;
    }
  }

  @media (max-width: 420px) {
    .wordmark {
      font-size: 0.92rem;
    }

    .site-nav {
      gap: 12px;
    }

    .site-nav a {
      font-size: 0.78rem;
    }
  }
</style>
